//! This script is used by linkChecker.ts to check if a link is safe or not.

import { Environment } from '../config.js';

export default async (url: string) => {
    //TODO: Implement this function to check if the link is safe or not
    //? use Google Safe Browsing API?

    return true;
};
